# QuickStart: Telegram + InMemory

1) Put your Telegram bot token into `appsettings.json`.
2) Run the project.
3) Send `/ping` or `/echo hello` to the bot.

If you consume Vermilion from NuGet (instead of source), you can use a single package:
- `DioRed.Vermilion`
