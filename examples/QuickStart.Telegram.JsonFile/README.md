# QuickStart: Telegram + JsonFile storage

1. Create a Telegram bot (via @BotFather) and copy its token.
2. Put the token into `appsettings.json` (`Vermilion:Telegram:BotToken`).
3. Run the project.

Chats will be persisted into a single file: `vermilion-chats.json`.
